<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.msc.mubeen.school_base">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Sign_in_EMP"  android:label="Login Teacher" />
        <activity android:name=".ADMIN" />
        <activity android:name=".SignUP_EMP" />
        <activity
            android:name=".NEWS.NEWS"
            android:label="School NEWS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NEWS.ADD_NEWS" />
        <activity
            android:name=".TeacherDashBoard"
            android:label="@string/title_activity_teacher_dash_board"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".STUDENT.ADD_STUDENT" />
        <activity
            android:name=".LOGIN_PARENT"
            android:label="Login Parent" />
        <activity android:name=".COMMON_LOGIN_PAGE" />
        <activity android:name=".PARENT_DASHBOARD" />
        <activity android:name=".STUDENT.STUDENT_RESULTS" />
    </application>

</manifest>